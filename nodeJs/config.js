const config = {
    // API endpoints
    urls: {
        auth: 'https://oauth.wildapricot.org/auth/token',
        apiUrl: 'https://api.wildapricot.org/v2/accounts'
    },
    
    // API credentials
    credentials: {
        apiKey: 'YOUR_API_KEY_HERE',
        accountId: 'YOUR_ACCOUNT_ID_HERE'
    }
};

module.exports = config;